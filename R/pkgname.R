#' @details
#' \code{\link{json2env}} is the only function in the package json2env.
#' If this function proves useful, it could be added to another package
#' such as \code{\link{jsonlite}} or in a language parsing utility package.
#' @keywords internal
"_PACKAGE"
